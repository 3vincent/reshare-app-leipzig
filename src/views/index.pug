extends layout

block content
  h1= title
  p Welcome to #{title}
  
  h1 ACTIVE USERS
  block content
    p My user are
    a(href="/users?name=armagan") #{ben}
    a(href="/users?name=ben") #{armagan}
    p 
    p My offers are