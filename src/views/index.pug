extends layout

block content
  .page 
    .container
      nav
      h1= title
      p Welcome to #{title}
      p &nbsp;
      //- p This #{offers.map(offer => offer.title).join(', ')}
      //- p This #{offers.length}
      p &nbsp;
      .row
        .col-sm-6
          //- each offer in offers
          h5 #{offer}
          ul.list-group
              li.list-group-item
                p Offer Description 
                a(href="/users?name=armagan") #{ben}
                a(href="/users?name=ben") #{armagan}
                p 
                p Location
                button.btn.btn-primary View Offer

        .col-sm-6
          h5 List Items 1
          ul.list-group
              li.list-group-item
                p Offer Description 
                a(href="/users?name=armagan") #{ben}
                a(href="/users?name=ben") #{armagan}
                p 
                p Location                
                button.btn.btn-primary View Offer

      .row   
        .col-sm-6 
        p &nbsp;
        p link to users
        